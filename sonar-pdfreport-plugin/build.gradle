

description = 'Sonar PDF Report (Sonar Plugin)'

dependencies {
    testCompile group: 'org.codehaus.sonar', name: 'sonar-testing-harness', version:'3.7'
    testCompile group: 'org.apache.maven', name: 'maven-project', version:'2.0'
    testCompile group: 'org.testng', name: 'testng', version:'5.7', classifier:'jdk15'
    compile(group: 'org.codehaus.sonar', name: 'sonar-plugin-api', version:'3.7') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
test.useTestNG()
